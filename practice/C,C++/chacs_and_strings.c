#include <stdio.h>

int main(){

    //Save escape characters in char variable with any name:
    /*char newLine = '\n';
    //the variable newLine is going to act like an escape character cuz it stores new line escape sequence code:
    printf("Hello, cursor next line please %c", newLine);
    printf("howdy!");*/

    return 0;
}