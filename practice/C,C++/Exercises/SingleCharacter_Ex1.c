#include <stdio.h>

int main(){

    //putchar function to print a single character:
    putchar('L');
    //Output a single character using the putc function to standard output stream which is the terminal:
    putc('K', stdout);
    //Can also send escape characters to the putc function:
    //sending new line character to the terminal:
    putc('\n', stdout);

    //Character 






    return 0;
}