#include <stdio.h>

//puts() function to print a string, already included in the standard library.
//also appends a new line to the output.

int main()
{
	char string[] = "Yet another string literal";

	puts("A string literal");
	puts(string);

	return(0);
}

